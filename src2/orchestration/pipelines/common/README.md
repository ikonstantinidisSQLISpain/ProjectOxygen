# Common

In serverless mode we can't import files from anywhere, they need to be inside pipeline root folde, this folder contains all the code that is copied and pasted into each individual pipeline that needs it.