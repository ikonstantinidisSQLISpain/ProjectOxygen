resources:
  pipelines:
    bronze_app:
      name: app
      configuration:
        read.catalog: ${var.catalog}
        read.schema: "landing"
        target.schema: "app"
      libraries:
        - glob:
            include: "../src/flow/pipelines/app/**"
      serverless: true
      catalog: ${var.catalog}
      schema: bronze_app