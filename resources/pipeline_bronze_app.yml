resources:
  pipelines:
    bronze_app:
      name: bronze_app
      configuration:
        read.catalog: ${var.catalog}
        read.schema: "landing"
        target.schema: "bronze_app"
      libraries:
        - glob:
            include: "./src/flow/pipelines/bronze_app/**"