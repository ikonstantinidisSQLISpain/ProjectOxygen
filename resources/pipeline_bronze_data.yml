resources:
  pipelines:
    bronze_data:
      name: bronze_data
      configuration:
        read.catalog: ${var.catalog}
        read.schema: "landing"
        target.schema: "bronze_data"
      libraries:
        - glob:
            include: "../src/flow/pipelines/bronze_data/**"
      serverless: true
      catalog: ${var.catalog}
      schema: bronze_data